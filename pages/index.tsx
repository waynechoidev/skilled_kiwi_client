import type { NextPage } from 'next';
import Head from 'next/head';
import { useState } from 'react';
import styles from '../styles/home.module.css';

const Home: NextPage = () => {
  const pageList = [
    { bg: '/img/card01.jpg', msg: '' },
    { bg: '/img/card02.jpg', msg: '' },
    { bg: '/img/card03.jpg', msg: '' },
  ];
  const [page, setPage] = useState(0);

  const turnPage = () => {
    if (page < pageList.length - 1) {
      setPage(page + 1);
    } else {
      setPage(0);
    }
  };

  return (
    <div className={styles.container}>
      <Head>
        <title>Skilled Kiwi || I need or I am</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <section className={styles.card} style={{ backgroundImage: `url(${pageList[page].bg})` }}>
        <ul className={styles.buttonWrap}>
          {pageList.map((page, index) => (
            <li
              key={index}
              onClick={() => {
                setPage(index);
              }}
            >
              ·
            </li>
          ))}
        </ul>
      </section>
    </div>
  );
};

export default Home;
